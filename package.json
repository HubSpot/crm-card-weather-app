{
  "name": "serverless-demo",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "deploy": "hs upload src serverless-demo && hs deploy serverless-demo/demo.functions",
    "dev": "hs watch src serverless-demo",
    "lint": "eslint . && prettier --list-different 'src/**/*.{js,json}'",
    "prettier:write": "prettier --write 'src/**/*.{js,json}'"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@hubspot/cms-cli": "^2.2.2"
  },
  "devDependencies": {
    "eslint": "^7.17.0",
    "husky": "^4.3.7",
    "lint-staged": "^10.5.3",
    "prettier": "^2.2.1"
  },
  "lint-staged": {
    "src/**/*.{js,json,scss,css}": [
      "prettier -l",
      "eslint"
    ]
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  }
}
